package wsu.csc5991.socially;

import android.location.Location;

public enum shared {
	// Define enum value
	Data;
		 
	// Declare enum fields
	public boolean backValue = true;
	public static Location currentLocation = null;
}
